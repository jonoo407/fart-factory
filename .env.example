# ElevenLabs API key — used ONLY by scripts/generate-sfx.ts at build time.
# Never bundled into the client. Never use the VITE_ prefix.
# Get a key at https://elevenlabs.io/ → Profile → API Keys.
ELEVENLABS_API_KEY=
